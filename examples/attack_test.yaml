name: http_basic_example_3
description: Test that example.com return HTTP 200
scrapeInterval: 1m
timeout: 1s
tags:
  sla: dev
scenario:
  kind: http
  steps:
    - type: request
      params:
        url: "http://localhost:8080"
    - type: statusCodeShouldBe
      params:
        statusCode: "200"